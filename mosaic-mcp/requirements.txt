# =============================================================================
# MOSAIC MCP Server - Optimized Dependencies
# =============================================================================
# This requirements file is optimized for smaller Docker image size
# Use with the multi-stage Dockerfile for best results
# =============================================================================

# AI & LLM
groq
langchain

# Vector Database & Search
chromadb
faiss-cpu  # For GPU: use faiss-gpu (requires nvidia-container-toolkit)

# Deep Learning & Transformers
# Note: Install PyTorch with CPU-only wheels in Dockerfile to save space
# torch --index-url https://download.pytorch.org/whl/cpu
transformers
sentence-transformers

# Computer Vision
# Using opencv-python-headless to avoid GUI dependencies (~500MB savings)
opencv-python-headless  # Instead of opencv-python
Pillow
numpy

# Video Processing
ffmpeg-python

# Utilities
python-dotenv
fastmcp
requests  # For health checks

# =============================================================================
# Size Optimizations Applied:
# =============================================================================
# 1. opencv-python-headless instead of opencv-python (-500MB)
# 2. PyTorch CPU-only wheels in Dockerfile (-2-3GB CUDA libraries)
# 3. Multi-stage build removes build dependencies (-500MB)
# Total savings: ~3-4GB (from 15GB to ~2.5-3GB)
# =============================================================================
